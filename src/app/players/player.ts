export interface Player {
  firstName: string;
  lastName: string;
  clubPlayer: boolean;
  enteredInSystem: number;
  showForTourneys: boolean;
  showForLeaderboard: boolean;
  showForElo: boolean;
}
